-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.category_attributes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  category_id uuid,
  attr_name text NOT NULL,
  attr_values ARRAY DEFAULT '{}'::text[],
  sort_order integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  attr_type text NOT NULL DEFAULT 'text'::text CHECK (attr_type = ANY (ARRAY['text'::text, 'number'::text, 'select'::text])),
  display_order integer NOT NULL DEFAULT 0,
  CONSTRAINT category_attributes_pkey PRIMARY KEY (id),
  CONSTRAINT category_attributes_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.category_templates(id)
);
CREATE TABLE public.category_templates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT category_templates_pkey PRIMARY KEY (id)
);
CREATE TABLE public.companies (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  vkn_tckn text NOT NULL UNIQUE,
  name text NOT NULL,
  tax_office text,
  address text,
  phone text,
  email text,
  website text,
  is_active boolean NOT NULL DEFAULT true,
  is_client boolean NOT NULL DEFAULT false,
  is_supplier boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  city text,
  postal_code text,
  trade_registry_no text,
  mersis_no text,
  tenant_id uuid,
  CONSTRAINT companies_pkey PRIMARY KEY (id),
  CONSTRAINT companies_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.dmo_order_items (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  order_id uuid NOT NULL,
  product_id uuid,
  quantity numeric NOT NULL DEFAULT 0,
  unit_price_excl_vat numeric DEFAULT 0,
  line_total_excl_vat numeric DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  is_gift boolean DEFAULT false,
  katalog_kod text,
  maliyet_usd numeric DEFAULT 0,
  maliyet_tl numeric DEFAULT 0,
  indirim_pct numeric DEFAULT 0,
  tenant_id uuid,
  CONSTRAINT dmo_order_items_pkey PRIMARY KEY (id),
  CONSTRAINT fk_dmo_order_items_order FOREIGN KEY (order_id) REFERENCES public.dmo_orders(id),
  CONSTRAINT fk_dmo_order_items_product FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT dmo_order_items_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.dmo_orders (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  sales_order_no text,
  purchase_order_no text,
  customer_no text,
  customer_name text,
  stamp_tax numeric DEFAULT 0,
  total_amount_excl_vat numeric DEFAULT 0,
  order_date date DEFAULT CURRENT_DATE,
  created_at timestamp with time zone DEFAULT now(),
  dmo_basket_total numeric DEFAULT 0,
  inokas_basket_total numeric DEFAULT 0,
  stamp_tax_total numeric DEFAULT 0,
  risturn_amount numeric DEFAULT 0,
  net_profit numeric DEFAULT 0,
  profit_percentage numeric DEFAULT 0,
  status text NOT NULL DEFAULT 'Beklemede'::text,
  usd_rate numeric DEFAULT 45,
  tevkifat numeric DEFAULT 0,
  gercek_kdv numeric DEFAULT 0,
  kdv_amount numeric DEFAULT 0,
  toplam_gelir numeric DEFAULT 0,
  toplam_gider numeric DEFAULT 0,
  gift_total numeric DEFAULT 0,
  pdf_url text,
  due_date date,
  real_dmo_basket numeric,
  tutar_indirimi numeric,
  tutar_indirimi_pct numeric,
  tenant_id uuid,
  CONSTRAINT dmo_orders_pkey PRIMARY KEY (id),
  CONSTRAINT dmo_orders_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.invoice_items (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  invoice_id uuid NOT NULL,
  product_name text NOT NULL,
  product_code text,
  brand_name text,
  unit_code text,
  dmo_code text,
  quantity numeric NOT NULL,
  unit_price_cur numeric NOT NULL,
  tax_rate numeric NOT NULL,
  total_price_cur numeric NOT NULL,
  currency text NOT NULL,
  order_reference_id text,
  sales_order_id text,
  order_line_id integer,
  is_internal boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  product_id uuid,
  purchase_order_item_id uuid,
  internal_category text,
  line_id integer,
  line_note text,
  manufacturer_code text,
  customs_tariff_code text,
  commodity_code text,
  tax_amount_cur numeric,
  tenant_id uuid,
  CONSTRAINT invoice_items_pkey PRIMARY KEY (id),
  CONSTRAINT invoice_items_invoice_id_fkey FOREIGN KEY (invoice_id) REFERENCES public.invoices(id),
  CONSTRAINT invoice_items_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT invoice_items_purchase_order_item_id_fkey FOREIGN KEY (purchase_order_item_id) REFERENCES public.purchase_order_items(id),
  CONSTRAINT invoice_items_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.invoices (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  efatura_uuid uuid NOT NULL UNIQUE,
  invoice_no text NOT NULL UNIQUE,
  company_id uuid NOT NULL,
  direction text NOT NULL CHECK (direction = ANY (ARRAY['INCOMING'::text, 'OUTGOING'::text])),
  invoice_date date NOT NULL,
  due_date date,
  currency text NOT NULL,
  total_tax_exclusive_tl numeric NOT NULL,
  tax_amount_tl numeric,
  payable_amount_tl numeric NOT NULL,
  status text NOT NULL DEFAULT 'Unpaid'::text,
  notes text,
  xml_url text,
  created_at timestamp with time zone DEFAULT now(),
  paid_amount numeric DEFAULT 0,
  invoice_type text,
  base_currency text,
  target_currency text DEFAULT 'TRY'::text,
  calculation_rate numeric DEFAULT 1,
  total_tax_exclusive_cur numeric,
  total_tax_inclusive_cur numeric,
  payable_amount_cur numeric,
  paid_amount_cur numeric DEFAULT 0,
  approval_status text NOT NULL DEFAULT 'approved'::text CHECK (approval_status = ANY (ARRAY['pending'::text, 'approved'::text, 'rejected'::text])),
  source text NOT NULL DEFAULT 'manual'::text CHECK (source = ANY (ARRAY['api'::text, 'manual'::text])),
  e_reply_status text,
  gib_status_code integer,
  gib_status_description text,
  pdf_url text,
  tenant_id uuid,
  company_name text,
  CONSTRAINT invoices_pkey PRIMARY KEY (id),
  CONSTRAINT invoices_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT invoices_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.payments (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  invoice_id uuid NOT NULL,
  amount numeric NOT NULL,
  currency text NOT NULL,
  payment_date date NOT NULL,
  notes text,
  created_at timestamp with time zone DEFAULT now(),
  tenant_id uuid,
  CONSTRAINT payments_pkey PRIMARY KEY (id),
  CONSTRAINT payments_invoice_id_fkey FOREIGN KEY (invoice_id) REFERENCES public.invoices(id),
  CONSTRAINT payments_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.product_attribute_values (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  product_id uuid,
  attribute_id uuid,
  value text,
  CONSTRAINT product_attribute_values_pkey PRIMARY KEY (id),
  CONSTRAINT product_attribute_values_attribute_id_fkey FOREIGN KEY (attribute_id) REFERENCES public.category_attributes(id),
  CONSTRAINT product_attribute_values_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.product_group_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  group_id uuid NOT NULL,
  product_code text NOT NULL,
  product_name text NOT NULL,
  default_quantity numeric NOT NULL DEFAULT 1,
  sort_order integer NOT NULL DEFAULT 0,
  CONSTRAINT product_group_items_pkey PRIMARY KEY (id),
  CONSTRAINT fk_pgi_group FOREIGN KEY (group_id) REFERENCES public.product_groups(id)
);
CREATE TABLE public.product_groups (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  group_name text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT product_groups_pkey PRIMARY KEY (id)
);
CREATE TABLE public.product_price_history (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid NOT NULL,
  dmo_fiyat_try numeric,
  sozlesme_fiyat_eur numeric,
  maliyet_usd numeric,
  recorded_at timestamp without time zone DEFAULT now(),
  purchase_price numeric,
  purchase_currency text,
  sales_price numeric,
  sales_currency text,
  CONSTRAINT product_price_history_pkey PRIMARY KEY (id),
  CONSTRAINT product_price_history_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.products (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_code text NOT NULL UNIQUE,
  product_name text NOT NULL,
  dmo_code text,
  stock_on_hand numeric DEFAULT 0,
  reserved_quantity numeric DEFAULT 0,
  ordered_quantity numeric DEFAULT 0,
  shipped_total numeric DEFAULT 0,
  last_purchase_price_cur numeric DEFAULT 0,
  last_purchase_currency text,
  last_purchase_rate numeric DEFAULT 0,
  last_purchase_price_tl numeric DEFAULT 0,
  avg_purchase_price_tl numeric DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  brand text,
  category text,
  model text,
  sozlesme_fiyat_eur numeric DEFAULT 0,
  dmo_fiyat_try numeric DEFAULT 0,
  dmo_fiyat_updated timestamp without time zone,
  maliyet_usd numeric DEFAULT 0,
  dmo_specs jsonb,
  dmo_notes jsonb,
  dmo_url text,
  gift_quantity numeric,
  needs_review boolean NOT NULL DEFAULT false,
  source text NOT NULL DEFAULT 'manual'::text,
  is_internal boolean NOT NULL DEFAULT false,
  tenant_id uuid,
  CONSTRAINT products_pkey PRIMARY KEY (id),
  CONSTRAINT products_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.purchase_order_items (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  purchase_order_id uuid NOT NULL,
  product_id uuid NOT NULL,
  ordered_qty numeric NOT NULL DEFAULT 0,
  received_qty numeric NOT NULL DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  unit_price_cur numeric CHECK (unit_price_cur IS NULL OR unit_price_cur >= 0::numeric),
  currency text,
  line_total_cur numeric CHECK (line_total_cur IS NULL OR line_total_cur >= 0::numeric),
  tenant_id uuid,
  CONSTRAINT purchase_order_items_pkey PRIMARY KEY (id),
  CONSTRAINT fk_poi_order FOREIGN KEY (purchase_order_id) REFERENCES public.purchase_orders(id),
  CONSTRAINT fk_poi_product FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT purchase_order_items_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.purchase_orders (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  po_number text NOT NULL UNIQUE,
  company_id uuid NOT NULL,
  order_date date DEFAULT CURRENT_DATE,
  status text NOT NULL DEFAULT 'Bekliyor'::text,
  notes text,
  created_at timestamp with time zone DEFAULT now(),
  tenant_id uuid,
  CONSTRAINT purchase_orders_pkey PRIMARY KEY (id),
  CONSTRAINT fk_po_company FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT purchase_orders_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.quote_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  quote_id uuid NOT NULL,
  sort_order integer NOT NULL DEFAULT 0,
  product_code text,
  product_name text NOT NULL,
  unit text NOT NULL DEFAULT 'ADET'::text,
  quantity numeric NOT NULL DEFAULT 1,
  unit_price numeric NOT NULL DEFAULT 0,
  total_price numeric NOT NULL DEFAULT 0,
  tenant_id uuid,
  CONSTRAINT quote_items_pkey PRIMARY KEY (id),
  CONSTRAINT quote_items_quote_id_fkey FOREIGN KEY (quote_id) REFERENCES public.quotes(id),
  CONSTRAINT quote_items_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.quotes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  reference_no text NOT NULL UNIQUE,
  company_id uuid,
  company_name text,
  quote_date date NOT NULL DEFAULT CURRENT_DATE,
  valid_until date,
  total_excl_tax numeric NOT NULL DEFAULT 0,
  currency text NOT NULL DEFAULT 'TRY'::text,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['draft'::text, 'pending'::text, 'accepted'::text, 'rejected'::text])),
  notes text,
  created_at timestamp with time zone DEFAULT now(),
  pdf_url text,
  tenant_id uuid,
  job_name text,
  CONSTRAINT quotes_pkey PRIMARY KEY (id),
  CONSTRAINT quotes_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT quotes_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.rate_history (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  usd_try numeric,
  eur_try numeric,
  dmo_eur_try numeric,
  recorded_at timestamp without time zone DEFAULT now(),
  rate_date date,
  dmo_rate_date date,
  tenant_id uuid,
  CONSTRAINT rate_history_pkey PRIMARY KEY (id),
  CONSTRAINT rate_history_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.sessions (
  token text NOT NULL,
  user_id uuid NOT NULL,
  tenant_id uuid NOT NULL,
  role text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  expires_at timestamp with time zone DEFAULT (now() + '7 days'::interval),
  CONSTRAINT sessions_pkey PRIMARY KEY (token),
  CONSTRAINT sessions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id),
  CONSTRAINT sessions_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.technical_issues (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  created_at timestamp with time zone DEFAULT now(),
  customer_name text,
  phone text NOT NULL,
  company_name text,
  product text,
  issue_description text,
  priority text DEFAULT 'normal'::text CHECK (priority = ANY (ARRAY['acil'::text, 'normal'::text, 'düşük'::text])),
  status text DEFAULT 'bekliyor'::text CHECK (status = ANY (ARRAY['bekliyor'::text, 'inceleniyor'::text, 'çözüldü'::text, 'kapatıldı'::text])),
  whatsapp_thread jsonb DEFAULT '[]'::jsonb,
  resolved_at timestamp with time zone,
  notes text,
  tenant_id uuid,
  CONSTRAINT technical_issues_pkey PRIMARY KEY (id),
  CONSTRAINT technical_issues_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.tenant_integrations (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  tenant_id uuid NOT NULL,
  provider text NOT NULL DEFAULT 'logo'::text,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT tenant_integrations_pkey PRIMARY KEY (id),
  CONSTRAINT tenant_integrations_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);
CREATE TABLE public.tenants (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name text NOT NULL,
  slug text NOT NULL UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  onboarding_complete boolean NOT NULL DEFAULT false,
  vkn text,
  CONSTRAINT tenants_pkey PRIMARY KEY (id)
);
CREATE TABLE public.users (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  tenant_id uuid NOT NULL,
  email text NOT NULL UNIQUE,
  password_hash text NOT NULL,
  role text NOT NULL DEFAULT 'admin'::text CHECK (role = ANY (ARRAY['admin'::text, 'user'::text])),
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT users_pkey PRIMARY KEY (id),
  CONSTRAINT users_tenant_id_fkey FOREIGN KEY (tenant_id) REFERENCES public.tenants(id)
);